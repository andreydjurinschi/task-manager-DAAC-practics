# cfg file
spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/task-manager_db
    username: admin
    password: pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
